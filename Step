Enter.
Complite tread in reprozitory.
Complite windows.h in reprozitory.
Declare streaming Glass in size equal 1000000000.
Double streaming Glass;
Declare const Num at the tread equal 3;
Doble Global the amount everyone const Num at the Tread;
Emptiness V (Declare const Num at the Tread);
Double p, the amount equal 0.0;
Streaming Glass qual 2.0;
Division Double Num at the tread;
During (Declare i equal Num at the tread; i less streaming Glass; i equal i add Num at the tread);
p equal (i add 1,5) multiply Glass;
The amount equal the amount multiply (p and V).
Global the amount (Num at the tread) the amount equal the amount.

Enter.
Declare line of search "DAO".
Apply t1 equal CurrentDb.
OpenRecordset "Сотрудники".
Index of t1 equal "PrimaryKey".
Seek of t1 "=", "Иванов".
Apply t2 equal CurrentDb.
Open Recordset ("Командировки", dbOpenSnapshot).
FindFirst of t2 "Кто = "Алексеев".
End line of search.
Declare line of notation "DAO".
Apply database equal CurrentDb.
Line SQL equal "SELECT multiply II".
From Exel1 WHERE firm LIKE multiply.
Apply query results equal OpenRecordset (line SQL).
MoveFirst of query results.
MsgBox Fields (0) of query results fasten "—" fasten of query results (2).
FindFirst of query results "Фирма" equal 'Intel'".
MsgBox Fields (0) of query results fasteb "—" fasten of query results.
FindNext of query results "Фирма = 'Intel'".
MsgBox RecordCount of query results.
End line of notation.

Enter.
Declare real numbers in V,T,n,E;
Start of programm
Apply ("Введите значение");
Apply ("V"); ReadLn (a);
Apply ("T"); ReadLn (T);
If V=0
Then 
If T=0
Then 
If n=0
Then WriteLn ("Любое x-решение");
Else WriteLn ("Нет решений");
Else 
Start p=2/3n
WriteLn ('p=';p);
End
Else 
Start
E:=pV/T
If E<0
Then
If E>0
WriteLn ('E='; E);
End
Start of programm:
Declare integer number m, v, g, h, E
Function Nod (m, v: Integer; var g, h, E: Integer);
Start
While m<>V then g:=g-h
Else E:=mV^2+mgh
Nod:=E
End;
Start 
White ("Введите значения");
Read (m, V, g, h);
E:= mV^2+mgh
End.



